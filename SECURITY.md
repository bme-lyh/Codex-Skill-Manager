# 安全策略

[English version](SECURITY_EN.md)

## 威胁模型

Skills 是不可信的指令包，可能含可执行脚本、提示注入、凭据访问、数据外传、破坏性命令、隐藏内容或供应链替换。

Codex Skill Manager 因此：

- 只接受 `https://github.com` 公共/私有来源或使用者明确选择的本地目录；
- 把可变分支和标签解析为不可变 Commit SHA；
- 防止压缩包路径穿越和符号链接逃逸；
- 只扫描实际准备安装或更新的 Skill 文件；
- 不执行仓库的安装、构建或初始化脚本；
- 活跃的 High/Critical 默认阻止写入；已知的 Medium 及以下风险可以逐簇或按报告
  忽略，High 必须逐簇确认并填写原因，Critical 不可忽略；目标和决定都会记录；
- 替换前保留备份，移除时进入隔离区；
- 永不修改 `.codex/skills/.system`；
- 从日志和报告中排除凭据；
- 默认纯本地，Codex 风险复核和辅助安装都需要使用者主动选择。

## Codex 功能

Codex 风险复核只提供建议，不会自动替使用者忽略风险。Codex 辅助安装会把固定
Commit 的完整 GitHub 暂存仓库或明确本地来源打包为模型输入：文本包含原文，
二进制包含路径、大小和 SHA-256；只有来源根目录中确认为真实元数据的
`.git`、`.hg`、`.svn` 会跳过，同名普通目录和 `.csm-backups`、
`.csm-quarantine` 不会成为逃避检查的隐藏区。大仓库会分块覆盖所有文本后统一
汇总。文件在读取期间发生替换、变为符号链接或逃逸来源根目录时，分析会立即停止。
该会话关闭 Shell 工具，工作目录只包含应用管理的输出文件。两者都需要已登录的
Codex CLI，会消耗额度；私有仓库使用者应在启用前确认数据要求，并避免把包含无关
备份的宽泛父目录选为本地来源。
设置中的总开关只控制安全中心风险复核；在界面选择辅助安装或在 CLI 使用
`--assist`，就是本次安装的明确启用。

Codex 的输出不是执行权限。辅助安装只允许本地验证过的类型化步骤：安装选中的
Skills、从官方 PyPI 安装与 GitHub 仓库归属匹配的固定版本 Wheel 工具，以及添加
由应用管理的 Codex MCP 配置。应用不会执行仓库脚本、任意 Shell、源码构建或
Codex 指定的写入路径。不能自动执行的内容会保留为人工步骤；受支持步骤可以先
完成，最终状态会明确显示“部分完成”，不会把人工步骤伪装成成功。

辅助分析发现 Python 工具时，会从官方 PyPI 下载仅含 Wheel 的完整依赖闭包到隔离
暂存区，再把名称、版本、文件名、兼容标签和 SHA-256 写入待批准计划。源码包不会
执行。包含本机代码的 Wheel 会派生独立的 `managed-native-code` 高风险权限；正式
执行完全离线，只接受已批准哈希。应用启动的 pip 会被强制通过一个临时本地代理，
该代理只允许连接 `pypi.org:443` 和 `files.pythonhosted.org:443`；继承的代理及
`PIP_*` 网络配置会被清除。TLS 仍由 pip 端到端验证，下载文件还必须与 PyPI 官方
元数据中的文件名、地址和 SHA-256 一致。

该出站限制保护的是应用启动的正常 Python/pip 流程，不是操作系统级网络沙箱。
如果本机 Python 或 pip 本身已被恶意替换，它仍可能绕过代理直接联网；因此辅助
安装只应使用可信的本机 Python 环境。

每个自动步骤都需要明确权限并写入事务。失败或取消会逆序恢复已完成的可逆步骤；
Codex MCP 配置会在事务检查点后及最终原子替换前再次核对。如果目标后来被修改，
写入或恢复都会停止而不是覆盖，并给出人工恢复路径。

## 扫描器限制

内置扫描是确定性的尽力检查。“未发现风险”只表示没有命中当前规则，不能证明 Skill 绝对安全。动态行为、新型提示攻击和运行时远程内容仍可能绕过静态分析。

## 私有仓库凭据

认证顺序为环境中的 `GITHUB_TOKEN`/`GH_TOKEN`、现有 `gh auth token`、Windows 凭据管理器。Token 不写入配置、日志或报告。

## 报告漏洞

在公开 Issue 中不要提交真实 Token、Cookie、私有仓库内容或可直接利用的未修复细节。请使用 GitHub 的私密漏洞报告功能；如果仓库尚未启用该功能，请仅公开提交不含敏感细节的联络 Issue。

报告应包含受影响版本、组件、使用非敏感测试数据的复现步骤、期望/实际行为和潜在影响。维护者会先确认收到，再协调修复和披露。

## 支持范围

安全修复优先应用于最新发布版本。旧版用户可能被要求先升级再验证。
