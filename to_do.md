# Codex Skill Manager 重构任务单

本文件是后续完整重构、测试和发布的执行依据。先以当前工作区代码为准核对版本和分支，再实施以下设计；不得破坏现有安全边界、事务日志、备份/隔离和恢复能力。

## 一、目标架构设计

### 1. 领域边界

- 安装和更新以一个来源分组（Source Group）为操作单位；每个分组绑定一个 GitHub 仓库、引用和解析后的不可变 Commit，可包含多个相关 Skills。
- 安装一次只处理一个分组，必须覆盖该组全部有效 Skills，后端拒绝部分成员集合；更新可以批量选择多个分组，但每个分组必须由独立父事务完整处理。
- Skill 级记录只保留为父事务的内部步骤、证据、失败和恢复信息，不建立可独立管理的主状态，不提供单 Skill 安装/更新入口。
- 状态主键继续使用 `root_id + source_group_id`，所有管理状态、更新状态、进度和历史均以分组为主。

### 2. 核心对象

实现或统一以下对象，并让持久化、API、前端模型使用同一契约：

- `SourceGroup`：仓库连接、分组 ID、来源路径、根目录、成员 Skills、默认分支/请求引用、解析 Commit、元数据和最近操作。
- `SourceTrustPolicy`：仓库级信任、授予/撤销时间、策略版本；信任范围覆盖整个仓库。
- `SourceAnalysis`：项目理解和整组安装方案，绑定仓库、Commit、源文件摘要、模型配置、策略版本和有效期。
- `GroupSecurityReport`：整组风险摘要、分层评估结果、总体决定和 Skill 级证据；保存中英文两份。
- `GroupApproval`：人工一键通过记录，绑定分组、仓库、Commit、安全报告和策略版本；不要求填写理由，但技术完整性错误不可绕过。
- `GroupOperation`：安装、更新、检查的父事务、状态、进度、子步骤、备份/恢复路径、错误和事件日志。
- `LocalizedText`：任何用户可见的分析、方案、权限说明、风险报告、结果和元数据均保存 `zh-CN`、`en-US` 两份。

### 3. 统一可复用流程

提供统一服务/用例，安装、更新和安全中心只调用这些模块，不在页面中复制流程：

`PrepareSourceGroup` → `UnderstandSourceGroup` → `BuildGroupInstallPlan` → `AssessGroupSecurity` → `ApproveGroupSecurity` → `ConfirmGroupPermissions` → `ApplyGroupOperation` → `PostGroupInstallCheck`

- 可信来源：理解项目和生成整组方案 → 权限确认（如需要）→ 安装/更新整组 → 整组安装后检查 → 保存元数据和报告。
- 不可信来源：理解项目并保存待审核方案 → 按需进行分层安全检查 → 展示整组报告 → 人工一键通过 → 展示可执行方案 → 权限确认 → 安装/更新整组 → 整组检查。
- 同一仓库 Commit 的理解结果和安装方案只生成一次；安装、更新、安全检查复用缓存，过期或摘要变化时重新生成。
- Codex 输出只能产生分析、计划和受限操作意图，不能直接授权任意命令；不执行仓库提供的脚本。

### 4. 分组状态和事务语义

分组主状态至少支持：

`unknown`、`preparing`、`analyzing`、`security-checking`、`awaiting-approval`、`installing`、`checking`、`completed`、`partial`、`failed`、`recovery-required`、`update-available`、`up-to-date`、`rate-limited`、`unsupported`。

- 一个 Skill 内部失败不得阻断其他 Skill 步骤；父事务最终为 `completed`、`partial` 或 `failed`，并保留完整恢复路径。
- 普通重试仍以整组为目标；恢复流程可只处理父事务中未完成的内部步骤。
- 所有变更必须有 dry-run/prepare、明确目标、事务日志、备份或隔离、结构化结果、失败报告和恢复入口。
- 路径越界、Commit/摘要变化、快照不完整、方案过期、无有效 Skill 根目录等技术错误不能通过“忽略风险”绕过。

### 5. 分组级接口

新增或统一以下接口，并让新 UI、CLI 和事务强制使用分组校验；旧单项接口只保留兼容层并拒绝不完整目标集合：

- `GetOrCreateSourceGroupAnalysis`
- `SetSourceTrust` / `RevokeSourceTrust`
- `RunGroupSecurityCheck`
- `ApproveGroupSecurity`
- `PrepareGroupInstall` / `ApplyGroupInstall`
- `PrepareGroupUpdate` / `ApplyGroupUpdate`
- `GetGroupMetadata` / `GetGroupOperation`

### 6. 来源解析和安装鲁棒性

- 统一 GitHub URL、分支、标签、子目录和本地路径解析，先解析到不可变 Commit。
- `.`、仓库根目录和子目录的规范化规则必须前后一致；`.` 不能被误当成一个 Skill 根路径。
- 发现阶段递归识别有效 `SKILL.md` 根目录，清晰区分“仓库可读取但没有有效 Skill”“路径不存在”“快照不完整”和“权限/网络失败”。
- 对空仓库、嵌套 Skills、重复名称、非法路径、超大文件、上下文分块失败和部分下载失败给出可恢复的结构化错误；不执行仓库脚本。
- 以 `virgiliojr94/book-to-skill` 做真实来源发现、整组分析、安全报告和整组安装回归，明确验证根目录/子目录解析、有效 Skill 发现和一键通过后的流程。

## 二、界面设计

### 1. 安装弹窗（一次一个分组）

- 主体只展示一个来源分组及其全部有效 Skills；可以查看成员详情，但没有选择、反选、删除成员或单 Skill 安装按钮。
- 按状态分步显示：来源与 Commit → 项目理解/整组方案 → 风险摘要 → 权限 → 安装进度 → 安装后检查 → 结果。
- 风险摘要和总体结论默认折叠，细节按 Skill 展开；安全策略阻止时先展示报告归纳和可展开证据，不直接只显示一句错误。
- “一键通过风险”放在底部操作栏左侧；右侧依次为“取消”和“安装整组”。按钮文案直接表达动作，不要求输入理由。
- 已通过的报告、方案、Commit 或策略版本失效时，明确提示重新检查，不把旧批准误用于新计划。

### 2. 安全中心

- 以分组行显示总体风险、Commit、报告时间、处理状态和“一键通过”按钮；按钮位于该分组行。
- 摘要和细节默认折叠，展开后显示 Skill 级证据、规则 ID、文件名、哈希和修复建议；证据原文不做润色。
- 通过后安装/更新直接复用同一份报告；技术错误仍显示不可绕过原因和恢复动作。

### 3. 更新页面

- 更新列表以分组为单位；每个分组默认折叠理解、方案和安全步骤，只在错误、风险、权限请求或人工步骤出现时自动展开。
- 更新按钮只更新整组；内部 Skill 结果作为详情显示，支持父事务级重试和恢复。
- 多个分组可并行运行，但每个分组独立显示状态、进度、错误和恢复入口，不能互相覆盖日志。

### 4. 分组管理和元数据

- 列表展示仓库、分组名称、成员数量、Commit、信任状态、最近分析/报告、安装和更新时间、当前分组状态。
- 选择分组后展示中英文项目理解、整组安装方案、权限说明、安全报告和历史；切换语言只切换已保存文本，不重复调用 Codex。
- 统一处理浅色/深色主题：弹窗、表单、复选框、按钮、侧栏、风险标签、加载和错误状态都使用主题 token，不能残留硬编码白底/深色文字；满足 Apple Human Interface Guidelines。
- 文案简洁、具体、可操作；固定界面、README、CHANGELOG、截图说明和发布说明经过 Humanizer 检查。

## 三、当前需要修改的重点

### P0：先修正流程和数据一致性

1. 将旧的 `selectedSkills`、单 Skill/多选分支和旧弹窗状态机替换为分组父事务状态机；安装页强制一次一个完整分组。
2. 安装、更新、安全中心全部接入统一的理解、方案、安全报告和批准服务；删除页面内重复的 Codex 调用和旧版安全策略短路。
3. 修复安全报告“只显示安全策略已阻止安装”的问题：阻止前必须先返回整组摘要、细节和明确的技术阻断原因；一键通过后重新校验当前报告/Commit/方案并继续同一父事务。
4. 修复批准与计划绑定不一致：批准绑定分组 + 仓库 + Commit + 报告 + 策略版本；计划变化、摘要变化或过期时拒绝复用旧批准并要求重新审核。
5. 修复 `book-to-skill` 安装失败：统一 `.`/仓库根目录解析，正确发现有效 `SKILL.md` 根目录；区分“没有有效 Skill”“路径错误”“权限/网络失败”和“安全技术阻断”，并加入真实回归测试。

### P1：分组状态、部分失败和恢复

1. 迁移/统一数据库和内存状态，使管理、更新、历史和进度只写分组主状态；Skill 结果仅写父事务子步骤。
2. 实现整组安装/更新的继续执行策略：单个内部步骤失败时继续其他步骤，最终准确生成 `completed`、`partial` 或 `failed`，并提供父事务重试、备份和恢复。
3. 增加应用重启恢复、并发互斥和操作围栏；允许不同分组并行，禁止同一分组重复操作，日志和取消信号隔离。

### P2：安全和大仓库鲁棒性

1. 将安全检查改为可缓存、分层、可重试的整组流程，支持超大文件和上下文分块；记录每块文件清单、期望/实际数量、重试次数和最终可恢复错误。
2. 保持路径边界、快照完整性、摘要变化和不执行脚本等安全不变量；Critical/High/Medium/Low 均可人工一键通过，但技术错误不可绕过。
3. 对空结果、部分结果、限流、网络超时和 Codex 不可用提供明确状态和重试入口，避免“尚未检查”长期残留。

### P3：前端体验和双语

1. 以共享组件重做安装、安全、更新弹窗和报告详情，统一折叠、步骤、按钮顺序、错误和加载状态。
2. 完成全量浅色/深色主题核查，修复复选框、弹窗组件、侧栏、风险标签和 Home/Groups 展示区的颜色与对比度。
3. 所有分析、方案、权限、报告、状态、元数据和恢复摘要一次生成中英文两份；补齐 locale 切换、缺失文本和历史数据迁移。
4. 扫描报告支持点击进入分组详情和 Skill 证据，不再只有不可交互列表。

### P4：测试、文档和交付

1. 补充后端、前端和端到端测试：整组约束、部分选择拒绝、可信/不可信来源、Critical 一键通过、批准复用、部分失败、恢复、重启、并发互斥、双语和大仓库分块。
2. 运行 `gofmt`、`go test ./...`、前端类型检查/Vite 构建、Wails 构建和真实 `book-to-skill` 来源测试；不执行来源仓库脚本。
3. 同步 README、CHANGELOG、用户文档、架构文档、CLI/API 契约、截图和 `Handoff.md`；截图统一 `1440×900`，长页面按滚动位置拆分。
4. 更新本地应用和构建清单，核对版本、提交摘要和 SHA-256；按仓库约定完成分支、PR、Squash merge、远端清理和发布。任何破坏性或无法从代码确认的决策先停下来确认。

## 四、验收标准

- 安装、更新、安全检查都以分组为主；安装始终覆盖整组，管理和更新状态没有独立 Skill 主状态。
- 不可信来源能够先看到整组安全摘要，人工一键通过后继续使用当前有效方案；旧批准、技术阻断和无效 Skill 根目录不会被错误绕过。
- `virgiliojr94/book-to-skill` 能完成来源发现、整组分析、安全报告和在批准后的整组安装，失败时能给出具体可恢复原因。
- 中英文、浅色/深色主题、折叠/展开、报告详情、部分失败和应用重启恢复均可验证。

请在必要时调用 codex-review-graph、luna-agent 和 humanizer 中的相关skills。
